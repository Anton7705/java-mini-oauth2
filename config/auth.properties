# Optional external overrides for the authorization server.
# Picked up automatically when ./config/auth.properties exists (see application-auth.properties).
# auth.secret=replace-with-a-long-random-secret
# auth.issuer=mini-auth
# auth.audience=payments-api
# auth.access-ttl-sec=900
# auth.refresh-ttl-days=14
# auth.clock-skew-sec=60
# auth.rate-limit.enabled=true
# auth.rate-limit.requests-per-minute=60
